#!/usr/bin/python
# tuple is similar to list, but it cannot be changed after it is defined
print 'just use parenthesis to define a tuple'
name = ('jesse', 'world')
print 'This time you cannot change anything to this tumple'
print 'But you can still retrive it using tuple[index]'
print name
print 'name[1] is', name[1]

print 'If a list instance is in a tuple, you can change the list and the tuple remain still.'

